language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gaijinpunch/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gaijinpunch/docker-react npm run test -- --coverage
